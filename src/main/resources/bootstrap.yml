spring:
  application:
    name: netflix-gateway
  profiles:
    active:
      dev
server:
  port: 9090
zuul:
  sensitiveHeaders: Cookie,Set-Cookie
  routes:
    user:
      path: /product/**
      serviceId: HELPPOINT-PRODUCT
